## แผนแก้ไขระบบ Phaopanya Master Data — ฉบับปฏิบัติการเต็มรูปแบบ

**ตัวเลขฐานที่ยืนยันแล้วจากไฟล์วิเคราะห์:** 383 แถวขัดแย้ง → W ถูก **269 (70.2%)** / O ถูก **106 (27.7%)** / ไม่ชี้ขาด **8 (2.1%)** · จังหวัดขัดแย้ง **40 แถว (0.33%)** ผ่านเกณฑ์ <50 · กลุ่ม O ว่าง **1,702 แถว (14.2%)** สะอาด **96.2%** (suspect แข็ง 11 / นุ่ม 15 / ชนบท 39)

**หลักการของแผนนี้:** ทำของที่ *ย้อนกลับได้* ก่อนของที่ *ย้อนกลับไม่ได้* เสมอ — และทุกเฟสมีเกณฑ์ Go/No-Go ชัดเจนก่อนข้ามไปเฟสถัดไป

---

# 📍 ภาพรวม 8 เฟส

| เฟส | ชื่อ | เวลา | ความเสี่ยง | ย้อนกลับได้? |
|---|---|---|---|---|
| **0** | เตรียมความพร้อม + สำรอง | 30 นาที | 🟢 ไม่มี | — |
| **1** | วิเคราะห์ชี้ขาด (Cross-tab) | 30 นาที | 🟢 ไม่มี | — |
| **2** | แก้ 106 แถวที่ข้อมูลราชการผิด | ครึ่งวัน | 🟢 ต่ำ | ✅ ได้ |
| **3** | ปิด Version Drift *(ประตูบังคับ)* | 1 ชม. | 🟡 ต่ำ-กลาง | ✅ ได้ |
| **4** | Dry-run เขตเขต + PII | ครึ่งวัน | 🟢 ไม่มี | — |
| **5** | Migration จริง | 1 วัน | 🔴 **สูงสุด** | ⚠️ ยาก |
| **6** | Deploy FIX-A + ซ่อม SelfTest | 1 ชม. | 🟡 กลาง | ✅ ได้ |
| **7** | แก้ต้นทาง SCG | ต่อเนื่อง | 🟢 ต่ำ | — |
| **8** | Guardrail ถาวร | ต่อเนื่อง | 🟢 ไม่มี | — |

---

# เฟส 0 — เตรียมความพร้อม *(30 นาที · ความเสี่ยง 0)*

> **ห้ามข้ามเฟสนี้เด็ดขาด** ทุกเฟสหลังจากนี้พึ่งพาสิ่งที่ทำในเฟสนี้

### ขั้นที่ 0.1 — สำรองชีตแบบเต็ม
1. เปิดชีต → `ไฟล์` → `ทำสำเนา`
2. ตั้งชื่อ: `BACKUP_FULL_ก่อนแก้_2026-09-22`
3. ย้ายไปโฟลเดอร์แยกที่ไม่มีใครแตะ
4. **ห้ามลบอย่างน้อย 90 วัน** (ไม่ใช่ 30 วัน เพราะเฟส 5 เสี่ยงสูง)

### ขั้นที่ 0.2 — สำรองแบบ CSV เพิ่มอีกชั้น
1. `ไฟล์` → `ดาวน์โหลด` → `CSV` เฉพาะชีต `MASTER_PLACE`
2. เก็บไว้นอก Google Drive (เครื่องตัวเอง / NAS)
3. **เหตุผล:** ถ้า Google Sheets เองมีปัญหา สำเนาใน Drive ก็พังตาม

### ขั้นที่ 0.3 — ประกาศหน้าต่างหยุดระบบ (Freeze Window)
1. แจ้งทุกคนที่ใช้ชีต: **ห้ามกดปุ่ม 1 / 3 / 3b** จนกว่าจะแจ้งเลิก
2. ปุ่ม 2 (งานประจำวัน) ยังใช้ได้ เพราะอ่านอย่างเดียว
3. จดเวลาเริ่ม freeze ไว้

### ขั้นที่ 0.4 — จดตัวเลขฐาน 8 ตัว *(ใช้วัดผลทุกเฟส)*

| # | ตัวชี้วัด | สูตร | ค่าเริ่มต้น |
|---|---|---|---|
| 1 | แถวทั้งหมด | `=COUNTA(A2:A)` | 11,961 |
| 2 | อำเภอขัดแย้ง | `=SUMPRODUCT((O2:O11962<>"")*(W2:W11962<>"")*(TRIM(O2:O11962)<>TRIM(W2:W11962)))` | 383 |
| 3 | จังหวัดขัดแย้ง | `=SUMPRODUCT((N2:N11962<>"")*(V2:V11962<>"")*(TRIM(N2:N11962)<>TRIM(V2:V11962)))` | 40 |
| 4 | O ว่าง | `=SUMPRODUCT((O2:O11962="")*(W2:W11962<>""))` | 1,702 |
| 5 | MATCH_KEY ซ้ำ | `=COUNTA(B2:B)-SUMPRODUCT(1/COUNTIF(B2:B,B2:B&""))` | 0 |
| 6 | เขตเขต ใน ADDR_CLEAN | `=SUMPRODUCT(--ISNUMBER(SEARCH("เขตเขต",D2:D11962)))` | 6,449 |
| 7 | GEO_LAYER ว่าง | `=COUNTBLANK(AA2:AA11962)` | 0 |
| 8 | MANUAL | `=COUNTIF(AA2:AA11962,"MANUAL")` | 21 |

### ขั้นที่ 0.5 — สร้างชีตบันทึกการแก้ไข
สร้างชีตใหม่ชื่อ `SYS_AUDIT_LOG` มีคอลัมน์:

```
TIMESTAMP | PHASE | MD_ID | COLUMN | OLD_VALUE | NEW_VALUE | REASON | OPERATOR | ROLLBACK_DONE
```

> **ทุกการแก้ไขในเฟส 2 และ 5 ต้องบันทึกที่นี่** — นี่คือสิ่งที่ทำให้ rollback เป็นไปได้จริง

### ✅ เกณฑ์ผ่านเฟส 0
- [ ] มีสำเนา 2 ชุด (Drive + เครื่อง)
- [ ] ทีมรับทราบ freeze window
- [ ] จดตัวเลขฐานครบ 8 ตัว
- [ ] มีชีต `SYS_AUDIT_LOG` พร้อมใช้

---

# เฟส 1 — วิเคราะห์ชี้ขาด *(30 นาที · ความเสี่ยง 0)*

> เฟสนี้ **ไม่แก้อะไรเลย** แต่ตัดสินทิศทางของทั้งระบบในระยะยาว

### ขั้นที่ 1.1 — Cross-tab `GEO_LAYER` × `KNN_VERDICT`
1. เปิดไฟล์ `Master_conflict_OW_383rows_2026-09-22.csv`
2. เลือกทั้งตาราง → `แทรก` → `ตาราง Pivot`
3. ตั้งค่า: **แถว** = `GEO_LAYER`, **คอลัมน์** = `KNN_VERDICT`, **ค่า** = `COUNTA` ของ `MD_ID`

### ขั้นที่ 1.2 — อ่านผลและตัดสินใจ

| ถ้าผลออกมา | แปลว่า | การตัดสินใจถาวร |
|---|---|---|
| **106 แถวผิด กระจุกที่ 1–2 ชั้น** | ป้าย layer ทำนายความผิดได้ | 🟢 **ไปต่อ `GEO_CONFIDENCE`** สร้าง auto quality gate ได้ |
| **กระจายเท่า ๆ กันทุกชั้น** | ป้าย layer ไม่มีค่าเชิงทำนาย | 🔴 **ทิ้งไอเดีย `GEO_CONFIDENCE` ถาวร** ใช้ kNN เป็นเกณฑ์คุณภาพแทน |

### ขั้นที่ 1.3 — ยืนยันความสัมพันธ์ 81 กับ 106
1. กรอง `KNN_VERDICT = O ถูก` ได้ 106 แถว
2. เทียบกับรายการ 81 แถวจากรายงานฉบับแรก
3. **ยืนยันว่า 81 ⊂ 106 หรือไม่**
4. ถ้าใช่ → ใช้ **106 เป็นตัวตั้ง** (อิงพิกัด แม่นกว่าอิงป้าย layer)
5. ถ้าไม่ใช่ → มีแถวนอกเซ็ต ต้องสอบสวนเพิ่มก่อนไปเฟส 2

### ขั้นที่ 1.4 — แบ่ง 106 แถวเป็น 2 กอง *(ขั้นตอนที่สำคัญที่สุดของเฟสนี้)*

เพิ่มคอลัมน์ `BATCH_GROUP` ในไฟล์ CSV:

```
=IF(TRIM(N2)=TRIM(V2),"A_อำเภอเท่านั้น","B_แตะระดับจังหวัด")
```

| กอง | จำนวนโดยประมาณ | ลักษณะ | วิธีจัดการ |
|---|---|---|---|
| **A** | ~85 แถว | จังหวัดตรงกัน ต่างแค่อำเภอ/ตำบล | ✅ วางทับอัตโนมัติได้ |
| **B** | ~21 แถว | จังหวัดต่างกัน | 🔴 **ต้องตรวจมือทีละแถว** |

> ⚠️ **เหตุผลที่ต้องแยก:** ระดับอำเภอ kNN ชี้ขาดได้ 70:28 แต่ระดับจังหวัดแบ่ง **21:18** เกือบครึ่งต่อครึ่ง — **แปลว่า kNN แทบไม่มีอำนาจตัดสินในกอง B** ห้าม auto-correct เด็ดขาด

### ✅ เกณฑ์ผ่านเฟส 1
- [ ] มี pivot table แล้ว และตัดสินเรื่อง `GEO_CONFIDENCE` ได้
- [ ] ยืนยันความสัมพันธ์ 81 ↔ 106 แล้ว
- [ ] แบ่งกอง A / B เรียบร้อย พร้อมจำนวนจริง

---

# เฟส 2 — แก้ 106 แถวที่ข้อมูลราชการผิด *(ครึ่งวัน · ความเสี่ยงต่ำ)*

> เฟสนี้ **ไม่แตะโค้ด ไม่แตะ `MATCH_KEY`** จึงย้อนกลับได้เสมอ

### ขั้นที่ 2.1 — ออกแบบโครงสร้าง Correction CSV

ใช้โครงนี้ทุกคอลัมน์ เพื่อให้วางทับได้ปลอดภัยและตรวจย้อนกลับได้:

| คอลัมน์ | ตัวอย่าง | หน้าที่ |
|---|---|---|
| `MD_ID` | MD-0003 | คีย์อ้างอิง **ห้ามแก้** |
| `ROW_NUM` | 4 | เลขแถวจริงในชีต |
| `BATCH_GROUP` | A / B | กองตามขั้นที่ 1.4 |
| `KNN_VERDICT` | O ถูก | คำตัดสิน |
| `KNN_VOTES` | 15/15 | ความมั่นใจ — **ต่ำกว่า 10/15 ให้ตรวจมือ** |
| `OLD_U` … `OLD_X` | 10110 / กรุงเทพฯ / คลองเตย / คลองเตย | ค่าเดิม **สำหรับ rollback** |
| `NEW_U` … `NEW_X` | 10170 / กรุงเทพฯ / ตลิ่งชัน / บางระมาด | ค่าใหม่ |
| `SOURCE_OF_NEW` | SYS_TH_GEO + kNN | ที่มาของค่าใหม่ |
| `NEW_GEO_LAYER` | MANUAL_KNN | ป้ายใหม่ ป้องกันถูกทับ |
| `REVIEWER` | อีเมลผู้ตรวจ | ความรับผิดชอบ |
| `APPLIED_AT` | (เว้นว่าง) | เติมเมื่อวางจริง |

### ขั้นที่ 2.2 — เติมค่าที่ถูกต้อง
1. ใช้ `PROVINCE` (N) + `AMPHOE` (O) ของแต่ละแถวเป็นตัวตั้ง
2. `VLOOKUP` เข้าชีต `SYS_TH_GEO` เพื่อดึง **ตำบล + รหัสไปรษณีย์** ที่ตรงกัน
3. ถ้าอำเภอนั้นมีหลายตำบล → **ปล่อยว่าง อย่าเดา** ให้ตรวจมือ
4. กรอก `NEW_U` ถึง `NEW_X` ให้ครบ

### ขั้นที่ 2.3 — Pilot 10 แถวแรก *(ห้ามข้าม)*
1. เลือก 10 แถวจาก**กอง A** ที่ `KNN_VOTES` สูงสุด
2. บันทึกค่าเดิมลง `SYS_AUDIT_LOG` ก่อน
3. วางค่าใหม่ลงคอลัมน์ U–X
4. ตั้ง `GEO_LAYER = MANUAL_KNN`

### ขั้นที่ 2.4 — ตรวจ Pilot ด้วยตา
1. เปิด Google Maps ใส่พิกัด `LAT, LNG` ของทั้ง 10 แถว
2. ยืนยันว่าอำเภอ/ตำบลใหม่ตรงกับที่ Maps แสดง
3. **ผ่าน 10/10** → ไปขั้นที่ 2.5
4. **ผิดแม้แต่ 1 แถว** → หยุด กลับไปทบทวนขั้นที่ 2.2

### ขั้นที่ 2.5 — วางกอง A ทั้งหมด (~85 แถว)
1. บันทึกค่าเดิมทุกแถวลง `SYS_AUDIT_LOG`
2. วางค่าใหม่ทีละ 20 แถว *(ไม่ใช่ทีเดียวทั้งหมด)*
3. หลังแต่ละชุด 20 แถว สุ่มตรวจ 2 แถวด้วย Maps
4. เติม `APPLIED_AT`

### ขั้นที่ 2.6 — จัดการกอง B ด้วยมือ (~21 แถว) 🔴
1. **ห้ามใช้สคริปต์ ห้ามวางทับอัตโนมัติ**
2. เปิด Google Maps ทีละแถวด้วยพิกัดจริง
3. ตัดสินด้วยตาว่าจังหวัดไหนถูก
4. บันทึกเหตุผลลงคอลัมน์ `REVIEW_NOTE`
5. ตั้ง `GEO_LAYER = MANUAL_KNN`
6. ถ้าไม่แน่ใจ → **ปล่อยไว้เดิม** แล้วติดธง `NEED_FIELD_CHECK`

### ขั้นที่ 2.7 — จัดการ 8 แถวไม่ชี้ขาด
ทำเหมือนกอง B ทุกประการ — ตรวจมือ ไม่แน่ใจปล่อยไว้

### ขั้นที่ 2.8 — ล็อกไม่ให้ถูกเขียนทับ
ตั้ง `GEO_LAYER = MANUAL_KNN` ทุกแถวที่แก้ เพราะ `shouldSkipEnglishGeo_` จะข้ามแถวที่มี layer อยู่แล้ว → **งานที่แก้ด้วยมือจะปลอดภัยถาวร**

### ขั้นที่ 2.9 — ตรวจผลรวม
1. รันสูตรตัวชี้วัด #2 ใหม่
2. **คาดหวัง:** 383 → ประมาณ **277** (383 − 106)
3. รันสูตร #3 (จังหวัด) → **คาดหวัง 40 → ~19**
4. รันสูตร #5 (คีย์ซ้ำ) → **ต้องยังเป็น 0**

### ✅ เกณฑ์ผ่านเฟส 2
- [ ] ตัวชี้วัด #2 ลดลงตามคาด
- [ ] ตัวชี้วัด #5 ยังเป็น 0
- [ ] `SYS_AUDIT_LOG` มีบันทึกครบทุกแถวที่แก้
- [ ] Pilot ผ่าน 10/10

---

# เฟส 3 — ปิด Version Drift 🚪 *(1 ชม. · ประตูบังคับ)*

> **ห้ามข้ามไปเฟส 4–5 ก่อนทำเฟสนี้** — เพราะถ้าแพตช์ `cleanThai` ผิดไฟล์ จะเกิด duplicate ทั้งชีต 11,961 แถว

### ขั้นที่ 3.1 — สำมะโนไฟล์ในชีตจริง
1. `ส่วนขยาย` → `Apps Script`
2. จดรายชื่อไฟล์ทั้งหมดจากแถบซ้าย
3. เปิดแต่ละไฟล์ อ่าน **10 บรรทัดแรก** จดเลขเวอร์ชัน
4. กรอกตาราง:

| ชื่อไฟล์ | เวอร์ชันที่หัวไฟล์ | มี FIX-A? | มี PATCH-6? | มี NO_MATCH? |
|---|---|---|---|---|
| `00_Config.gs` | | | | |
| `00_CleanService.gs` | | | | |
| `01_MasterService.gs` | | | | |
| `04_GeoService.gs` | | ← **สำคัญสุด** | | |
| `99_SelfTest.gs` | v5.5.1 | — | — | — |

### ขั้นที่ 3.2 — เทียบกับไฟล์ในแพ็กเกจ
เทียบทีละไฟล์ว่าชีตกับแพ็กเกจต่างกันตรงไหน ใช้เครื่องมือเทียบข้อความ (diff) หรือเทียบด้วยตาที่หัวไฟล์ก็พอ

### ขั้นที่ 3.3 — ตัดสิน Source of Truth
1. เลือกว่า **"โค้ดในชีต"** คือความจริงเพียงหนึ่งเดียว
2. ไฟล์ในแพ็กเกจทั้งหมดถือเป็น *สำเนา* ไม่ใช่ต้นฉบับ
3. เขียนกติกานี้ลงใน README ให้ชัด

### ขั้นที่ 3.4 — ติดตั้ง Version Stamp

```javascript
// ── 00_Config.gs ──────────────────────────────
const SCRIPT_VERSION = 'v5.5.4';  // ← ใส่เลขจริงจากขั้นที่ 3.1
const VERSION_DATE   = '2026-09-22';

// ── logRun_ (แก้ให้ประทับเวอร์ชันทุกครั้ง) ──────
function logRun_(fn, msg) {
  const stamp = `[${SCRIPT_VERSION}] ${fn}`;
  // เขียนลงชีต 'การตั้งค่า' ตามเดิม โดยใช้ stamp แทน fn
}
```

### ขั้นที่ 3.5 — ทดสอบว่าประทับจริง
1. กดปุ่มอะไรก็ได้ที่เรียก `logRun_`
2. เปิดชีต `การตั้งค่า`
3. ต้องเห็น `[v5.5.4] ชื่อฟังก์ชัน` ปรากฏ

### ขั้นที่ 3.6 — สร้างไฟล์ `VERSION.md`
บันทึก: เวอร์ชันที่รันจริง / วันที่ deploy / รายการแพตช์ที่ติดตั้งแล้ว / รายการที่ยังไม่ติดตั้ง

### ✅ เกณฑ์ผ่านเฟส 3 — **ประตูบังคับ**
- [ ] รู้แน่ชัดว่าชีตรันเวอร์ชันอะไร
- [ ] `SCRIPT_VERSION` ปรากฏใน log จริง
- [ ] มี `VERSION.md` แล้ว
- [ ] รู้ว่า `FIX-A` ติดตั้งแล้วหรือยัง

---

# เฟส 4 — Dry-run เขตเขต + PII *(ครึ่งวัน · ความเสี่ยง 0)*

> เฟสนี้ **สร้างรายงานอย่างเดียว ไม่แตะข้อมูลจริง**

### ขั้นที่ 4.1 — ร่างชุดกฎ clean

```javascript
// กฎที่ 1 — ตัดคำซ้ำ เขตเขต / แขวงแขวง / ต.ต.
s = s.replace(/เขต\s*เขต/g, 'เขต')
     .replace(/แขวง\s*แขวง/g, 'แขวง')
     .replace(/ต\.?\s*ต\./g, 'ต.');

// กฎที่ 2 — ตัดคำว่า โทร. ออกจากชื่อ
s = s.replace(/โทร\.?\s*/g, ' ');

// กฎที่ 3 — ตัดกลุ่มตัวเลขยาว >= 9 ตัว (เบอร์โทร 296 แถว)
s = s.replace(/[\d\-\s]{9,}/g, ' ');

// กฎที่ 4 — Unicode normalize (แก้สระซ้อนที่ตาเห็นเหมือนแต่ต่าง byte)
s = s.normalize('NFC');

// กฎที่ 5 — บีบช่องว่างซ้ำให้เหลือตัวเดียว
s = s.replace(/\s+/g, ' ').trim();
```

> 📌 **โค้ด DN/พนักงาน 538 แถว** (`-dn 2250916715`, `s260101457`) — **อย่าลบ** ให้ย้ายไปคอลัมน์ note ใหม่ เพราะอาจเป็นข้อมูลอ้างอิงที่ทีมใช้จริง

### ขั้นที่ 4.2 — สร้างรายงาน before/after
1. รันกฎทั้ง 5 ข้อกับข้อมูลสำเนา (ไม่ใช่ของจริง)
2. สร้างไฟล์ xlsx แสดงทุกแถวที่เปลี่ยน:

```
MD_ID | OLD_NAME | NEW_NAME | OLD_ADDR | NEW_ADDR | OLD_KEY | NEW_KEY | RULE_APPLIED | COLLISION_FLAG
```

### ขั้นที่ 4.3 — ตรวจ collision *(เกณฑ์ตัดสินที่สำคัญที่สุด)*
1. นับว่า `NEW_KEY` ซ้ำกันเองกี่คู่
2. **ค่าที่คาดหวัง: 0** (ตรงกับที่วัดไว้ว่า key เปลี่ยน 6,451 แต่ duplicate ใหม่ 0)
3. **ถ้าได้ > 0 → หยุดทันที** ต้องทบทวนกฎใหม่ทั้งชุด

### ขั้นที่ 4.4 — ตรวจด้วยตา 50 แถว
1. สุ่ม 50 แถวจากรายงาน
2. อ่าน before/after ทีละคู่
3. มองหาการตัดที่มากเกินไป เช่น ชื่อร้านที่มีเลขจริง (`ร้าน 1000speed`) ถูกตัดเลขทิ้ง
4. **ถ้าเจอเคสเสียหาย → ปรับกฎ แล้วรัน dry-run ใหม่**

### ขั้นที่ 4.5 — ประเมินผลที่คาดว่าจะได้

| ตัวชี้วัด | ก่อน | คาดหวังหลังแก้ |
|---|---|---|
| เขตเขต ใน ADDR_CLEAN | 6,449 | **0** |
| O ว่าง | 1,702 | **~742** (ปลดล็อก 960) |
| เบอร์โทรใน NAME_CLEAN | 296 | **0** |
| MATCH_KEY ซ้ำ | 0 | **ต้องยังเป็น 0** |

### ✅ เกณฑ์ผ่านเฟส 4 — Go/No-Go Gate
- [ ] collision = 0
- [ ] ตรวจตา 50 แถวไม่พบความเสียหาย
- [ ] เฟส 3 ผ่านแล้ว (รู้เวอร์ชันแน่ชัด)
- [ ] **ถ้าข้อใดข้อหนึ่งไม่ผ่าน → ห้ามไปเฟส 5**

---

# เฟส 5 — Migration จริง 🔴 *(1 วัน · ความเสี่ยงสูงสุด)*

> **นี่คือการผ่าตัดหัวใจของระบบ** — ต้องแก้โค้ดและข้อมูลพร้อมกันในหน้าต่างเดียว

### ขั้นที่ 5.1 — Checklist ก่อนเริ่ม *(ต้องครบทุกข้อ)*
- [ ] เฟส 0–4 ผ่านครบ
- [ ] มีสำเนา 2 ชุด และทดสอบเปิดได้จริง
- [ ] รู้เวอร์ชันที่รันแน่ชัด
- [ ] collision dry-run = 0
- [ ] มีคนว่างทั้งวัน ไม่มีงานด่วนแทรก
- [ ] **ไม่ใช่วันศุกร์เย็น** (ถ้าพังจะไม่มีคนแก้เสาร์อาทิตย์)

### ขั้นที่ 5.2 — ปิดระบบชั่วคราว
1. แจ้งทีมว่าเข้าสู่ maintenance window
2. ห้ามใช้ทุกปุ่มรวมถึงปุ่ม 2
3. จดเวลาเริ่ม

### ขั้นที่ 5.3 — แพตช์ `cleanThai()` ในโค้ดที่รันจริง
1. เปิดไฟล์ `00_CleanService.gs` **ในชีต** (ไม่ใช่ในแพ็กเกจ)
2. เพิ่มกฎทั้ง 5 ข้อจากขั้นที่ 4.1
3. อัปเดต `SCRIPT_VERSION` เป็นเลขใหม่ เช่น `v5.5.4-clean1`
4. **บันทึก แต่ยังไม่รันอะไร**

### ขั้นที่ 5.4 — Batch update MASTER
1. รันสคริปต์ที่เขียน `NAME_CLEAN`, `ADDR_CLEAN`, `MATCH_KEY` ใหม่ทั้ง 11,961 แถว
2. **ทำทีละ 2,000 แถว** เพื่อไม่ชน `TIME_GUARD`
3. หลังแต่ละชุด เช็คตัวชี้วัด #5 (คีย์ซ้ำ) ต้องยังเป็น 0
4. บันทึกทุกการเปลี่ยนลง `SYS_AUDIT_LOG`

### ขั้นที่ 5.5 — Rebuild `SYS_MASTER_IDX`
1. รันคำสั่ง rebuild ดัชนี
2. ตรวจว่าจำนวนแถวใน IDX = จำนวนใน MASTER
3. **ข้อนี้ห้ามลืม** ไม่งั้นปุ่ม 2 จะแมตช์ไม่เจอทั้งระบบ

### ขั้นที่ 5.6 — ตรวจความสอดคล้องกับ SOURCE ⚠️
1. สุ่ม 20 แถวใน SOURCE ที่มี `MD_LINK`
2. ยืนยันว่ายังชี้ไปที่ `MD_ID` เดิมถูกต้อง
3. ทดลองนำเข้าข้อมูลใหม่ 5 แถวด้วยปุ่ม 1
4. **ยืนยันว่าไม่สร้างเรคคอร์ดซ้ำ** — นี่คือบททดสอบที่สำคัญที่สุดของเฟสนี้

### ขั้นที่ 5.7 — ตรวจผลรวมทั้งหมด

| ตัวชี้วัด | เกณฑ์ผ่าน |
|---|---|
| แถวทั้งหมด | **ยังเป็น 11,961** (ห้ามเพิ่ม/ลด) |
| MATCH_KEY ซ้ำ | **0** |
| เขตเขต | **0** |
| ปุ่ม 1 ทดลองนำเข้า | **ไม่สร้างซ้ำ** |
| LAT/LNG | **ไม่เปลี่ยนแม้แต่แถวเดียว** |

### ขั้นที่ 5.8 — แผน Rollback
**ทริกเกอร์ที่ต้อง rollback ทันที:**
1. คีย์ซ้ำ > 0
2. จำนวนแถวเปลี่ยน
3. ปุ่ม 1 สร้างเรคคอร์ดซ้ำ

**วิธี rollback:**
1. หยุดทุกอย่างทันที อย่าพยายามแก้ต่อ
2. เปลี่ยนชื่อชีตปัจจุบันเป็น `BROKEN_[เวลา]`
3. เปิดสำเนาจากเฟส 0.1 ขึ้นมาใช้แทน
4. Restore โค้ดเวอร์ชันก่อนแพตช์
5. วิเคราะห์สาเหตุ **ก่อน** ลองใหม่

### ขั้นที่ 5.9 — เปิดระบบ
1. แจ้งเลิก freeze window
2. เฝ้าดู 3 วันแรกอย่างใกล้ชิด
3. รันตัวชี้วัดทั้ง 8 ตัวทุกวันในสัปดาห์แรก

---

# เฟส 6 — Deploy FIX-A + ซ่อม SelfTest *(1 ชม.)*

### ขั้นที่ 6.1 — ติดตั้ง `04_GeoService_v5.5.8`
1. ทำเฉพาะเมื่อขั้นที่ 3.1 พบว่ายังไม่มี `FIX-A`
2. คัดลอกเนื้อหาไฟล์ v5.5.8 ทั้งหมด
3. วางทับไฟล์ `04_GeoService` ในชีต
4. อัปเดต `SCRIPT_VERSION` เป็น `v5.5.8`

### ขั้นที่ 6.2 — ทดสอบ FIX-A
1. หาแถวที่ `GEO_LAYER` ว่างหรือ `NO_MATCH` มา 20 แถว
2. รันปุ่ม 3 เฉพาะกลุ่มนี้
3. นับว่ากี่แถวได้ข้อมูลเพิ่ม — คือผลของ dedupe fix

### ขั้นที่ 6.3 — แพตช์ `99_SelfTest`
```javascript
// ❌ เดิม — พังเพราะ postal เป็น Number
if (typeof postal === 'string' && postal.length === 5) { ... }

// ✅ ใหม่ — บังคับแปลงเป็น String ก่อนเช็ค
const p = String(postal || '').trim();
if (/^\d{5}$/.test(p)) { ... }
```

### ขั้นที่ 6.4 — รัน SelfTest ยืนยัน
1. **คาดหวัง:** `testPostalCoverage_` เปลี่ยนจาก 0.0% → **99–100%**
2. ถ้ายังได้ 0% → เทสต์อ่านผิดคอลัมน์ ต้องแก้ mapping ด้วย

---

# เฟส 7 — แก้ต้นทาง SCG *(ต่อเนื่อง)*

> **3 ปัญหานี้มีรากเดียวกัน** — แก้ที่ต้นทางจุดเดียวปิดได้ทั้งหมด

### ขั้นที่ 7.1 — รวมหลักฐาน 3 กลุ่ม

| กลุ่ม | จำนวน | อาการ |
|---|---|---|
| ที่อยู่พิมพ์ผิด/เก่า | **269** | O ผิด พิกัดถูก |
| คำซ้ำ เขตเขต | **960** | บล็อกการ parse |
| ตัดกลางคัน | **22** | `แขวงบาง` / `ดอนเมือ` |

### ขั้นที่ 7.2 — จัดทำเอกสารเสนอเจ้าของระบบต้นทาง
1. ยกตัวอย่างจริง 10 เคสพร้อมภาพหน้าจอ
2. ระบุผลกระทบเชิงตัวเลข: **1,251 แถว (10.5%)**
3. เสนอแก้ที่การประกอบสตริงที่อยู่ในระบบ SCG

### ขั้นที่ 7.3 — ถ้าแก้ต้นทางไม่ได้ → สร้าง Sanitizer ชั้นรับ
1. เพิ่มฟังก์ชันล้างข้อมูลทันทีที่รับจาก SOURCE
2. ใช้กฎชุดเดียวกับเฟส 4.1
3. **ข้อเสีย:** ต้องเช็ดทุกเดือนตลอดไป ไม่หยุดเลือดถาวร

---

# เฟส 8 — Guardrail ถาวร *(ต่อเนื่อง)*

### ขั้นที่ 8.1 — สูตรเฝ้าระวังรายสัปดาห์
วางสูตรตัวชี้วัดทั้ง 8 ตัวไว้ในชีตใหม่ `SYS_HEALTH` แล้วดูทุกวันจันทร์

### ขั้นที่ 8.2 — kNN audit รายเดือน
รันสคริปต์ kNN geo-vote เดือนละครั้งกับแถวใหม่ที่เพิ่มเข้ามา

### ขั้นที่ 8.3 — Dashboard สุขภาพระบบ
แสดง: แถวใหม่ / แถวขัดแย้ง / แถว MANUAL / เวอร์ชันที่รัน / วันที่ rebuild IDX ล่าสุด

### ขั้นที่ 8.4 — กติกาเอกสาร AS-IS / TO-BE
ทุกเอกสารต่อจากนี้ต้องติดป้าย: ✅ `[VERIFIED]` / ⚠️ `[UNVERIFIED]` / 💡 `[PROPOSAL]`

### ขั้นที่ 8.5 — ตัดสินใจเรื่อง `ROLE_MAP`
เติมอีเมล หรือถอดโครงทิ้ง — **อย่าปล่อยคาไว้** ให้คนเข้าใจผิดว่ามีระบบสิทธิ์

---

# 📊 ตารางความเสี่ยงและการรับมือ

| # | ความเสี่ยง | เฟส | ผลกระทบ | การรับมือ |
|---|---|---|---|---|
| R1 | คีย์ซ้ำหลัง migration | 5 | 🔴 วิกฤต | dry-run collision = 0 + สำรอง 2 ชุด |
| R2 | แพตช์ผิดไฟล์เพราะ version drift | 5 | 🔴 วิกฤต | **เฟส 3 เป็นประตูบังคับ** |
| R3 | auto-correct จังหวัดผิดในกอง B | 2 | 🟠 สูง | แยกกอง B ตรวจมือ 100% |
| R4 | ตัดข้อมูลจริงทิ้งด้วยกฎ regex | 4–5 | 🟠 สูง | ตรวจตา 50 แถวก่อนวาง |
| R5 | ลืม rebuild IDX | 5 | 🟠 สูง | ใส่ใน checklist ขั้น 5.5 |
| R6 | งานที่แก้มือถูกเขียนทับ | 2 | 🟡 กลาง | ตั้ง `GEO_LAYER = MANUAL_KNN` |
| R7 | ชน TIME_GUARD กลางทาง | 5 | 🟡 กลาง | แบ่ง batch 2,000 แถว |
| R8 | ข้อมูลใหม่ไหลเข้าระหว่างแก้ | ทุกเฟส | 🟡 กลาง | freeze window |

---

# ⏱️ ไทม์ไลน์แนะนำ

| วัน | เฟส | ผลลัพธ์ที่ได้ |
|---|---|---|
| **วันที่ 1 เช้า** | 0 + 1 | รู้ทิศทาง `GEO_CONFIDENCE` + แบ่งกอง A/B |
| **วันที่ 1 บ่าย** | 2 | 106 แถวถูกต้อง ขัดแย้งลดเหลือ ~277 |
| **วันที่ 2 เช้า** | 3 | **ปิด version drift** — ประตูเปิด |
| **วันที่ 2 บ่าย** | 4 | รายงาน dry-run พร้อมตัดสิน Go/No-Go |
| **วันที่ 3 ทั้งวัน** | 5 | migration เสร็จ เขตเขต = 0 |
| **วันที่ 4 เช้า** | 6 | FIX-A + SelfTest เขียว |
| **สัปดาห์ถัดไป** | 7 + 8 | แก้ต้นทาง + ตั้ง guardrail |

---

## 🎯 3 สิ่งที่ต้องจำให้ขึ้นใจ

1. **เฟส 3 คือประตู** — ห้ามแตะโค้ดใด ๆ ก่อนรู้ว่าชีตรันเวอร์ชันอะไร มิฉะนั้นเสี่ยง duplicate ทั้ง 11,961 แถว
2. **กอง B (~21 แถว) ห้าม auto-correct** — kNN ตัดสินระดับจังหวัดได้แค่ 21:18 แทบเสมอกัน ต้องใช้สายตาคนเท่านั้น
3. **collision = 0 คือเส้นแบ่งเป็นตาย** — ถ้า dry-run ได้มากกว่า 0 แม้แต่คู่เดียว ห้ามเข้าเฟส 5

---

อยากให้ผมช่วยทำส่วนไหนต่อดีครับ — **ร่างสคริปต์ batch update สำหรับเฟส 5.4 พร้อมระบบ checkpoint กัน TIME_GUARD** หรือจะให้ผมจัดแผนทั้งหมดนี้เป็นไฟล์ Word/Excel ให้เอาไปใช้เป็นเช็คลิสต์ติ๊กตามระหว่างทำงานจริงดีครับ? 🙂